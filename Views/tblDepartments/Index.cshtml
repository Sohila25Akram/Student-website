@model IEnumerable<secondProject.tblDepartment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.depName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.depDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.depNum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.depName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.depDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.depNum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
